{% comment %} fcy-sg-table — size table block {% endcomment %}

{% schema %}
{
  "name": "Size table",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "placeholder": "Women's tops" },
    { "type": "richtext", "id": "table_html", "label": "Table content", "info": "Build your table in the rich text editor." }
  ]
}
{% endschema %}
